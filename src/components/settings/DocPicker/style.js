import styled from "styled-components/native";
import { Button } from "react-native-elements";

export const OpenFilesButton = styled(Button).attrs(() => ({
  buttonStyle: {
    backgroundColor: "#333"
  }
}))``;
